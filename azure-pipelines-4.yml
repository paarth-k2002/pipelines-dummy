trigger:
- main

pool:
  vmImage: ubuntu-latest

stages:
- stage: Build
  jobs:
  - job: Build
    steps:
    - script: |
        echo "Building..."
        sleep 60  # Adding a 60-second wait to simulate build time
      displayName: 'Build step'
